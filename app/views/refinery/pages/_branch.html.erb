<ul>
    <% pages.where(:sitemap => true).each do |page| %>
        <li><%= link_to page.title, refinery.url_for(page.url) %>
            <%= render 'branch', pages: page.children unless page.children.empty? %>
        </li>
    <% end %>    
</ul>